name: Decompress Binaries

on:
  push:
    paths:
      - '**.apk'
      - '**.ipa'
      - '**.exe'
      - '**.deb'
      - '**.zip'
  workflow_dispatch: # Permite ejecutarlo manualmente desde la pestaña Actions

jobs:
  decompress:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y p7zip-full dpkg-dev

      - name: Find and Decompress Files
        run: |
          mkdir -p extracted_files
          
          # Buscar archivos binarios recursivamente
          find . -type f \( -name "*.apk" -o -name "*.ipa" -o -name "*.exe" -o -name "*.deb" -o -name "*.zip" \) | while read -r file; do
            echo "Procesando archivo: $file"
            filename=$(basename -- "$file")
            extension="${filename##*.}"
            dirname="extracted_files/${filename}_extracted"
            mkdir -p "$dirname"
            
            case "$extension" in
              apk|ipa|zip)
                echo "Extrayendo archivo tipo ZIP/APK/IPA..."
                unzip -q "$file" -d "$dirname" || echo "Aviso: Algunos errores ocurrieron al descomprimir $filename"
                ;;
              exe)
                echo "Extrayendo ejecutable PE (EXE) con 7z..."
                7z x "$file" -o"$dirname" -y
                ;;
              deb)
                echo "Extrayendo paquete Debian (DEB)..."
                dpkg-deb -x "$file" "$dirname"
                ;;
              *)
                echo "Formato no soportado directamente: $extension"
                ;;
            esac
          done

      - name: Upload Extracted Files
        uses: actions/artifacts@v4
        with:
          name: decompressed-outputs
          path: extracted_files/
          retention-days: 5
